# Copy this file to .env and fill in a real secret before running docker compose up.
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Host port to expose the app on (default 8000).
HOST_PORT=8000

# Set to false only for local HTTP testing without TLS. Keep true in production.
SECURE_COOKIES=true
